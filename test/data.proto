syntax="proto3";
package test;

import "tagger/tagger.proto";

message Data {
    string val = 1 [(tagger.tags) = "graphql:\"name11,optional\" bson:\"name12,omitempty\"" ];

    message nested {
        string __val2_value = 1 [(tagger.tags) = "graphql:\"name21,optional\" bson:\"name22,omitempty\"" ];

        message OneMore_Nested {
            string val3 = 1;
        }
    }
}
